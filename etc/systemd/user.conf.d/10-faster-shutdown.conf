# The same for services inside the user session. Without this they keep the
# 90s default while the manager above them is killed after 10s, so they would
# never get a clean stop -- the whole cgroup would be killed instead.
[Manager]
DefaultTimeoutStopSec=5s
