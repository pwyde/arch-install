# Ninety seconds is a long time to watch a stuck unit at shutdown. This only
# governs units that never set a TimeoutStopSec of their own; anything that
# legitimately needs longer, such as a database or a container runtime,
# declares its own and keeps it.
[Manager]
DefaultTimeoutStopSec=5s
