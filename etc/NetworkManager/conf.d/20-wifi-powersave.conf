# Power save lets the radio nap between beacons, which costs tens to hundreds
# of milliseconds of latency on an otherwise idle link for a fraction of a watt.
# Some Intel firmware (BE200/BE211) drops the link outright when it naps.
# 2 is "disable"; see nm-settings-nmcli(5).
[connection]
wifi.powersave = 2
