# On s2idle systems the ACPI RTC alarm is needed for suspend-then-hibernate to
# wake the machine up to hibernate. Installed only when /sys/power/mem_sleep
# reports s2idle as the active suspend mode.
rtc_cmos.use_acpi_alarm=1
